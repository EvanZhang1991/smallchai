{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:client": "lerna run --scope=smallchai-lowcode-client --stream dev",
    "start:server": "lerna run --scope=smallchai-lowcode-serve --stream start:dev"
  },
  "devDependencies": {
    "lerna": "^6.6.2"
  }
}
