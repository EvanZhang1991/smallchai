{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:client": "lerna run --scope=smallchai-lowcode-client --stream dev"
  },
  "devDependencies": {
    "lerna": "^6.6.2"
  }
}
